# Database Configuration
# You can use a full connection string or individual connection parameters
# DATABASE_URL=postgres://user:password@hostname:port/dbname

DB_HOST=your-db-host
DB_PORT=6543
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=postgres
DB_SSL=true

# Server Configuration
PORT=3000
NODE_ENV=production
JWT_SECRET=your-secure-random-string

# API Keys
SERPER_API_KEY=your-serper-api-key

# Security
# Comma-separated list of allowed origins (e.g., http://your-admin-dashboard.com)
ALLOWED_ORIGINS=
